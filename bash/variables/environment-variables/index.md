---
metaTitle: Переменные окружения в Bash — export, scope и безопасное использование
metaDescription: Узнай как работают переменные окружения в Bash: export, наследование дочерними процессами, .env и best practices.
author: Олег Марков
title: Переменные окружения в Bash — практический разбор
preview: Пошагово разберём переменные окружения в Bash: как задавать, экспортировать и использовать их в скриптах и CI.
---

## Что такое переменная окружения

Переменная окружения — это именованное значение, которое доступно процессу и его дочерним процессам.

## Локальная переменная vs переменная окружения

```bash
name="app"
export name="app"
```

## Способы задания

```bash
APP_ENV=prod LOG_LEVEL=warn ./deploy.sh
```

```bash
export APP_ENV=prod
./deploy.sh
```

## Наследование в subprocess

```bash
export TOKEN="abc123"
bash -c 'echo "$TOKEN"'
```

## Безопасность

- Не коммить секреты в репозиторий.
- Не выводи секреты в логи.
- Храни чувствительные значения в CI secrets или vault.

## Загрузка `.env`

```bash
set -a
source .env
set +a
```

## Итоги

Переменные окружения — стандартный способ конфигурации скриптов. Используй `export` осознанно и отделяй код от конфигурации.
