---
metaTitle: Как создавать функции в Python
metaDescription: Изучите создание функций в Python — синтаксис, аргументы, возвращаемые значения и примеры использования для структурирования кода.
author: Олег Марков
title: Как создавать функции в Python
preview: Разберем создание функций в Python — синтаксис, аргументы, возвращаемые значения и практические примеры для структурирования кода.
---

## Введение

Функции — базовый инструмент структурирования кода в Python. Они позволяют объединять повторяющийся код, упрощать обработку данных и улучшать читаемость программы.

В этой статье мы разберемся, как создавать функции в Python, использовать аргументы, возвращать значения и строить гибкие, переиспользуемые компоненты кода.

Если вы хотите детальнее изучить работу с функциями в Python — приходите на наш курс [Основы Python](https://purpleschool.ru/course/python-basics?utm_source=knowledgebase&utm_medium=article&utm_campaign=Kak_sozdavat_funkcii_v_Python). На курсе 209 уроков и 34 упражнения, AI-тренажеры для практики с кодом и задачами 24/7, решение задач с живым ревью наставника, еженедельные встречи с менторами.

## Создание функции

Функция определяется с помощью ключевого слова `def`:

```python
def greet():
    print("Hello, Python!")
```

Вызов функции:

```python
greet()  # Hello, Python!
```

## Функции с аргументами

Функция может принимать входные данные для обработки:

```python
def greet_user(name):
    print(f"Hello, {name}!")

greet_user("Alice")  # Hello, Alice!
```

## Возврат значений

Для передачи результата из функции используется `return`:

```python
def add(a, b):
    return a + b

result = add(3, 5)
print(result)  # 8
```

Функция может возвращать несколько значений через кортеж:

```python
def min_max(numbers):
    return min(numbers), max(numbers)

minimum, maximum = min_max([1, 5, 3])
print(minimum, maximum)  # 1 5
```

## Документация функции

В Python рекомендуется использовать docstring для описания функции:

```python
def multiply(a, b):
    """Возвращает произведение двух чисел."""
    return a * b
```

Чтение документации:

```python
print(multiply.__doc__)  # "Возвращает произведение двух чисел."
```

## Частые ошибки

* Определение функции после вызова → `NameError`.
* Пропуск `return` при необходимости вернуть значение.
* Использование изменяемых объектов как аргументов без учета побочных эффектов.

## Часто задаваемые вопросы

1. **Можно ли вызвать функцию внутри другой функции?**
   Да, функции могут вызывать друг друга или самих себя (рекурсия).

2. **Можно ли задать пустую функцию?**
   Да, с использованием `pass`:

```python
def empty():
    pass
```

3. **Что произойдет, если не использовать return?**
   Функция вернет `None`.

## Заключение

Функции в Python упрощают повторное использование кода и делают программы более читаемыми. Создание функций с аргументами, возвращаемыми значениями и корректной документацией — ключ к чистому и понятному коду.

Для системного изучения работы с функциями и аргументами в Python рекомендую пройти курс [Основы Python](https://purpleschool.ru/course/python-basics?utm_source=knowledgebase&utm_medium=article&utm_campaign=Kak_sozdavat_funkcii_v_Python). В первых 3 модулях уже доступно бесплатное содержание — начните погружаться в мир Python прямо сегодня.
