---
metaTitle: Функции в Bash — объявление, аргументы и переиспользование кода
metaDescription: "Узнай как писать функции в Bash: синтаксис, параметры, коды возврата и организация больших скриптов."
author: Олег Марков
title: Функции в Bash — полный практический разбор
preview: Научимся выделять логику в функции, передавать параметры и поддерживать Bash-скрипты в структурированном виде.
---

## Зачем функции

Функции убирают дублирование, делают скрипт модульным и читаемым.

## Объявление функции

```bash
greet() {
  echo "Привет, $1"
}

greet "Мир"
```

## Локальные переменные

```bash
build_archive() {
  local src="$1"
  local out="$2"
  tar -czf "$out" "$src"
}
```

## Коды возврата

```bash
is_ready() {
  curl -fsS "$1" >/dev/null
}

if is_ready "https://example.com/health"; then
  echo "OK"
else
  echo "FAIL"
fi
```

## Итоги

Структурируй скрипт через функции и придерживайся контракта: входные параметры + понятный exit status.
