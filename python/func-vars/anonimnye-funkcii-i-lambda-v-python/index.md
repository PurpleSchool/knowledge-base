---
metaTitle: Анонимные функции и lambda в Python
metaDescription: Изучите анонимные функции и выражения `lambda` в Python — синтаксис, применение, особенности и примеры использования в разных сценариях.
author: Олег Марков
title: Анонимные функции и lambda в Python
preview: Разберём анонимные функции в Python и выражения `lambda` — как их использовать для компактного кода и обработки данных.
---

## Введение

Анонимные функции в Python, или `lambda`, позволяют создавать функции без имени. Они часто применяются для упрощения кода, передачи функций в качестве аргументов и обработки данных в коллекциях.

В этой статье мы разберём синтаксис `lambda`, особенности использования и типичные сценарии применения в Python.

Если вы хотите глубже освоить функции в Python, включая lambda и замыкания — приходите на наш курс [Основы Python](https://purpleschool.ru/course/python-basics?utm_source=knowledgebase&utm_medium=article&utm_campaign=Anonimnye_funkcii_i_lambda_v_Python). На курсе 209 уроков и 34 упражнения, AI-тренажеры для практики с кодом и задачами 24/7, решение задач с живым ревью наставника, еженедельные встречи с менторами.

## Синтаксис lambda

Анонимная функция создаётся с помощью ключевого слова `lambda`:

```python
lambda аргументы: выражение
```

Пример:

```python
add = lambda x, y: x + y
print(add(5, 3))  # 8
```

Особенности:

* Содержит только одно выражение.
* Возвращает значение выражения автоматически.
* Не имеет собственного имени, если его не присвоить переменной.

## Применение lambda

### Сортировка списка словарей

```python
users = [{"name": "Alice", "age": 25}, {"name": "Bob", "age": 20}]
users.sort(key=lambda u: u["age"])
print(users)  # [{'name': 'Bob', 'age': 20}, {'name': 'Alice', 'age': 25}]
```

### Использование с функцией map

```python
numbers = [1, 2, 3, 4]
squared = list(map(lambda x: x**2, numbers))
print(squared)  # [1, 4, 9, 16]
```

### Фильтрация данных с filter

```python
numbers = [1, 2, 3, 4, 5]
even_numbers = list(filter(lambda x: x % 2 == 0, numbers))
print(even_numbers)  # [2, 4]
```

### Сведение данных с reduce

```python
from functools import reduce

numbers = [1, 2, 3, 4]
sum_all = reduce(lambda x, y: x + y, numbers)
print(sum_all)  # 10
```

## Частые ошибки

* Слишком сложные выражения внутри lambda делают код нечитаемым.
* Попытка использовать многострочные конструкции.
* Злоупотребление lambda там, где лучше обычная функция.

## Часто задаваемые вопросы

1. **Можно ли использовать lambda вместо обычной функции всегда?**
   Нет, для сложной логики лучше использовать обычные функции с `def`.

2. **Можно ли использовать lambda с замыканиями?**
   Да, lambda может ссылаться на переменные внешней области.

3. **Как отлаживать lambda-функции?**
   Используйте присвоение переменной и тестирование через print или в интерактивной сессии.

## Заключение

Анонимные функции и `lambda` — удобный инструмент для компактного кода и функционального программирования в Python. Они часто применяются для обработки коллекций и передачи функций как аргументов.

Для системного изучения функций и lambda рекомендую пройти курс [Основы Python](https://purpleschool.ru/course/python-basics?utm_source=knowledgebase&utm_medium=article&utm_campaign=Anonimnye_funkcii_i_lambda_v_Python). В первых 3 модулях уже доступно бесплатное содержание — начните погружаться в мир Python прямо сегодня.
